# Vectarine CLI

The vectarine CLI can be used to programmatically manage projects and run tests.

Goals:

- [ ] Work as a library for the editor to store export functions and project creation
- [ ] Run a project and generate a screenshot.
- [ ] Run a project and get console output
