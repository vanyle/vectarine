[package]
name = "vectarine-plugin-sdk"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["lib"]

[dependencies]
anyhow = "1.0.100"
egui = "0.36.2"
egui_glow = "0.36.2"
toml = "0.9.8"
rapier2d = "0.35.3"
lazy_static = "1.5.0"
serde = { version = "1.0.221", features = ["derive"] }

[target.'cfg(target_os = "windows")'.dependencies]
sdl2 = { version = "0.38", default-features = false, features = ["static-link", "use-vcpkg"] }
# Targets Luau 0.736
mlua = { git = "https://github.com/mlua-rs/mlua", version = "0.12.1", features = ["luau-jit", "serde", "vendored", "async"] }

[target.'cfg(any(target_os = "macos"))'.dependencies]
sdl2 = { version = "0.38", default-features = false, features = ["static-link", "use-vcpkg"] }
mlua = { git = "https://github.com/mlua-rs/mlua", version = "0.12.1", features = ["luau-jit", "serde", "vendored", "async"] }

[target.'cfg(any(target_os = "linux"))'.dependencies]
sdl2 = { version = "0.38", default-features = false, features = ["static-link", "bundled"] }
mlua = { git = "https://github.com/mlua-rs/mlua", version = "0.12.1", features = ["luau-jit", "serde", "vendored", "async"] }

[target.'cfg(target_os = "emscripten")'.dependencies]
sdl2 = { version = "0.38", default-features = false, features = [] }
mlua = { git = "https://github.com/mlua-rs/mlua", version = "0.12.1", features = ["luau", "serde", "vendored", "async"] }

[package.metadata.vcpkg]
dependencies = ["sdl2"]
git = "https://github.com/microsoft/vcpkg"

# To check for a new release, check https://github.com/microsoft/vcpkg/releases
# https://github.com/microsoft/vcpkg/commit/50c0cb48a0cf2f6fc5c7b2c0d2bafbe26d0a7ca2
rev = "50c0cb4"
